generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Sala {
  id        Int     @id @default(autoincrement())
  sala      String  @unique
  title     String
  vlan      Int
  isBlocked Boolean
}
